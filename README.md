# 1 - when the branch,that is being merged has not diverged from the target branch (no other commits in target branch)

# 2 - Rebase is used mostly for local branches to not show useless commtits,which sometimes can cause a danger situation,whether merge is a safe way,that stores all commits history of both branches

# 3 - I only left one version of change from main commint (ignored feautre-conflict commit)